# These are Python packages only — pip can't install Tesseract itself or
# its language data (needed for OCR_LANGS, default "hye+rus+eng"). Install
# those at the OS level first:
#   macOS:           brew install tesseract poppler
#                     brew install tesseract-lang   # adds Armenian/Russian/etc.
#   Ubuntu/Debian:    sudo apt-get install tesseract-ocr poppler-utils
#                     sudo apt install tesseract-ocr-hye tesseract-ocr-rus
#   Windows:          install Tesseract from the UB Mannheim build and check
#                     "Additional language data" (Armenian/Russian) during
#                     setup — or, if you already installed without it,
#                     download hye.traineddata / rus.traineddata from
#                     https://github.com/tesseract-ocr/tessdata and drop
#                     them into Tesseract-OCR/tessdata.
# See the README for the full per-OS walkthrough.
pdfminer.six>=20201018
pytesseract>=0.3.10
pdf2image>=1.16.0
Pillow>=8.0.0
pillow-heif>=0.15.0
pymupdf>=1.24.0
opencv-python-headless>=4.9.0
numpy>=1.24.0
openpyxl>=3.1.0
python-docx>=1.1.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
